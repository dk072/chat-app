### Automatic Deployment (Git Push)
Whenever you successfully complete a coding task, apply a bug fix, or create a new feature that modifies the codebase, you must automatically run `git add`, `git commit -m "..."`, and `git push origin main` before ending your turn. Do not wait for the user to explicitly tell you to push the changes.
